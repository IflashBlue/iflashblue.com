{{ form_start(form) }}
<div class="row g-3">
    <div class="col-12">
        {{ form_row(form.password) }}
    </div>
    <div class="col-12">
        <input type="submit" class="btn btn-primary" value="{{ 'admin.actions.reset'|trans }}">
    </div>
</div>
{{ form_end(form) }}
